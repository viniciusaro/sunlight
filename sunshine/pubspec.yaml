name: sunshine
description: "A new Flutter package project."
version: 0.0.1
repository: https://github.com/viniciusaro/sunshine

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: 1.3.0
  http_parser: ^4.0.0
  mutex: ^3.1.0
  path_provider: ^2.1.5
  screenshot: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0